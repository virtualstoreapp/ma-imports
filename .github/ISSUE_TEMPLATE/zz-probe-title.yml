# TEMPORARY probe. Isolates the title prefix with its trailing space.
name: Probe title prefix
description: Temporary diagnostic
title: "Novo produto: "
body:
  - type: input
    id: probe
    attributes:
      label: Anything
    validations:
      required: true
