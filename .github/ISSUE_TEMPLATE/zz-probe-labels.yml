# TEMPORARY probe. Isolates the labels: key naming a label that does not exist in the repo.
name: Probe labels key
description: Temporary diagnostic
labels: ["novo-produto"]
body:
  - type: input
    id: probe
    attributes:
      label: Anything
    validations:
      required: true
