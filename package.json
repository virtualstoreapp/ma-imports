{
    "scripts": {
        "start": "node serve.js",
        "build": "node tools/build.js",
        "preview": "npm run build && node serve.js dist",
        "test": "jest --verbose",
        "dev": "nodemon"
    },
    "jest": {
        "testEnvironment": "jest-environment-jsdom",
        "testPathIgnorePatterns": [
            "/node_modules/",
            "/.aiox-core/",
            "/dist/"
        ]
    },
    "devDependencies": {
        "@testing-library/dom": "^10.4.0",
        "@testing-library/jest-dom": "^6.6.3",
        "express": "^5.1.0",
        "jest": "^29.7.0",
        "jest-environment-jsdom": "^29.7.0",
        "jsdom": "^26.0.0",
        "nodemon": "^3.1.10",
        "sharp": "^0.35.3"
    }
}
